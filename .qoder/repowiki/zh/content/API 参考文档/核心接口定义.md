# 核心接口定义

<cite>
**本文档引用的文件**
- [context.ts](file://src/context.ts)
- [main.ts](file://src/main.ts)
- [runner.ts](file://src/runner.ts)
- [docxInput.ts](file://src/tasks/docxInput.ts)
- [docxConvert.ts](file://src/tasks/docxConvert.ts)
- [mediaConvert.ts](file://src/tasks/mediaConvert.ts)
- [mdCleanup.ts](file://src/tasks/mdCleanup.ts)
- [pandocCheck.ts](file://src/tasks/pandocCheck.ts)
- [utils.ts](file://src/utils.ts)
- [package.json](file://package.json)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖分析](#依赖分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)

## 简介

本文档详细说明了 doc2xml-cli 项目中的核心接口定义，特别是 AppContext 和 OutputContext 接口的设计理念、属性定义、类型系统以及在实际工作流中的使用方式。该工具是一个交互式的 CLI 管道，用于将 .docx 文档转换为 Markdown 格式，同时处理媒体文件和清理生成的 Markdown 内容。

## 项目结构

该项目采用模块化设计，主要由以下核心部分组成：

```mermaid
graph TB
subgraph "核心模块"
CTX[context.ts<br/>接口定义]
RUN[runner.ts<br/>运行器创建]
MAIN[main.ts<br/>入口点]
end
subgraph "任务模块"
DOCXIN[docxInput.ts<br/>输入处理]
PANDOC[pandocCheck.ts<br/>环境检查]
DOCXCONV[docxConvert.ts<br/>文档转换]
MEDCONV[mediaConvert.ts<br/>媒体处理]
MDCLEAN[mdCleanup.ts<br/>内容清理]
end
subgraph "工具模块"
UTILS[utils.ts<br/>缓存管理]
end
MAIN --> CTX
MAIN --> RUN
RUN --> DOCXIN
RUN --> PANDOC
RUN --> DOCXCONV
RUN --> MEDCONV
RUN --> MDCLEAN
DOCXIN --> UTILS
DOCXCONV --> CTX
MEDCONV --> CTX
MDCLEAN --> CTX
```

**图表来源**
- [main.ts:1-41](file://src/main.ts#L1-L41)
- [context.ts:1-21](file://src/context.ts#L1-L21)
- [runner.ts:1-10](file://src/runner.ts#L1-L10)

**章节来源**
- [package.json:1-40](file://package.json#L1-L40)
- [main.ts:1-41](file://src/main.ts#L1-L41)

## 核心组件

### AppContext 接口

AppContext 是整个系统的核心上下文接口，定义了应用程序运行所需的所有状态信息。

#### 属性定义

| 属性名 | 类型 | 必填 | 默认值 | 描述 |
|--------|------|------|--------|------|
| inputPath | string | 是 | 空字符串 | 用户输入的 .docx 文件绝对路径 |
| outputPath | string | 是 | 空字符串 | 总输出目录，与输入在同一级目录下的out目录 |
| pandocExec | string | 是 | 'pandoc' | 解析后的 pandoc 可执行文件路径 |
| lastContext | OutputContext | 否 | undefined | 上一个任务的输出上下文 |

#### 初始化方法

```mermaid
sequenceDiagram
participant Main as 主程序
participant Context as Context模块
participant Runner as Runner模块
Main->>Context : createContext()
Context->>Context : 创建默认AppContext对象
Context-->>Main : 返回{inputPath : '', outputPath : '', pandocExec : 'pandoc'}
Main->>Runner : createRunner(ctx)
Runner-->>Main : 返回Listr实例
```

**图表来源**
- [context.ts:18-20](file://src/context.ts#L18-L20)
- [runner.ts:4-9](file://src/runner.ts#L4-L9)

**章节来源**
- [context.ts:7-16](file://src/context.ts#L7-L16)
- [context.ts:18-20](file://src/context.ts#L18-L20)

### OutputContext 接口

OutputContext 接口定义了单个任务的输出状态，用于在任务间传递数据。

#### 属性定义

| 属性名 | 类型 | 必填 | 描述 |
|--------|------|------|------|
| outFilename | string | 是 | 输出文件名 |
| outputPath | string | 是 | 输出文件的完整路径 |
| mediaPath | string | 是 | 媒体文件目录路径 |

#### 使用场景

OutputContext 主要用于以下场景：
- 在文档转换任务完成后，记录转换结果的文件信息
- 在媒体处理任务中，传递处理后的媒体文件路径
- 在内容清理任务中，维护最终输出文件的状态

**章节来源**
- [context.ts:1-5](file://src/context.ts#L1-L5)

## 架构概览

系统采用流水线架构，通过 Listr2 库实现任务编排。每个任务都可以访问和修改 AppContext 中的状态。

```mermaid
flowchart TD
Start([应用启动]) --> CreateCtx[创建AppContext]
CreateCtx --> CreateRunner[创建Runner]
CreateRunner --> AddTasks[添加任务序列]
AddTasks --> Task1[docxInputTask<br/>输入验证]
Task1 --> Task2[pandocCheckTask<br/>环境检查]
Task2 --> Task3[docxConvertTask<br/>文档转换]
Task3 --> Task4[mediaConvertTask<br/>媒体处理]
Task4 --> Task5[mdCleanupTask<br/>内容清理]
Task1 --> UpdateCtx1[更新inputPath<br/>更新outputPath]
Task2 --> UpdateCtx2[设置pandocExec]
Task3 --> UpdateCtx3[设置lastContext]
Task4 --> UpdateCtx4[更新lastContext]
Task5 --> UpdateCtx5[更新lastContext]
UpdateCtx1 --> NextTask
UpdateCtx2 --> NextTask
UpdateCtx3 --> NextTask
UpdateCtx4 --> NextTask
UpdateCtx5 --> NextTask
NextTask --> End([任务完成])
```

**图表来源**
- [main.ts:9-16](file://src/main.ts#L9-L16)
- [docxInput.ts:43-49](file://src/tasks/docxInput.ts#L43-L49)
- [pandocCheck.ts:17-21](file://src/tasks/pandocCheck.ts#L17-L21)
- [docxConvert.ts:52-56](file://src/tasks/docxConvert.ts#L52-L56)
- [mediaConvert.ts:95-99](file://src/tasks/mediaConvert.ts#L95-L99)
- [mdCleanup.ts:362-366](file://src/tasks/mdCleanup.ts#L362-L366)

## 详细组件分析

### 输入处理任务 (docxInputTask)

输入处理任务负责获取用户输入的 .docx 文件路径，并设置基础的上下文信息。

```mermaid
sequenceDiagram
participant User as 用户
participant Task as docxInputTask
participant FS as 文件系统
participant Utils as 缓存工具
User->>Task : 启动输入任务
Task->>Utils : loadCache()
Utils-->>Task : 返回缓存的docx路径
Task->>User : 显示输入提示
User->>Task : 输入.docx路径
Task->>FS : 验证文件存在性
FS-->>Task : 验证结果
Task->>Task : 处理绝对/相对路径
Task->>Task : 设置inputPath和outputPath
Task-->>User : 显示成功消息
```

**图表来源**
- [docxInput.ts:27-51](file://src/tasks/docxInput.ts#L27-L51)

**章节来源**
- [docxInput.ts:27-51](file://src/tasks/docxInput.ts#L27-L51)

### 文档转换任务 (docxConvertTask)

文档转换任务使用 pandoc 将 .docx 文件转换为 Markdown 格式。

```mermaid
sequenceDiagram
participant Task as docxConvertTask
participant FS as 文件系统
participant Pandoc as Pandoc进程
participant Ctx as AppContext
Task->>FS : 创建输出目录
FS-->>Task : 创建成功
Task->>Pandoc : 启动转换进程
Pandoc->>Pandoc : 执行转换
Pandoc-->>Task : 返回状态码
alt 转换成功
Task->>Ctx : 设置lastContext
Ctx->>Ctx : 更新outFilename
Ctx->>Ctx : 更新outputPath
Ctx->>Ctx : 更新mediaPath
Task-->>Task : 转换完成
else 转换失败
Task-->>Task : 抛出错误
end
```

**图表来源**
- [docxConvert.ts:10-63](file://src/tasks/docxConvert.ts#L10-L63)
- [docxConvert.ts:52-56](file://src/tasks/docxConvert.ts#L52-L56)

**章节来源**
- [docxConvert.ts:10-63](file://src/tasks/docxConvert.ts#L10-L63)

### 媒体处理任务 (mediaConvertTask)

媒体处理任务负责将 EMF/WMF 矢量图转换为 JPG 格式，并更新 Markdown 文件中的引用。

```mermaid
flowchart TD
Start([开始媒体处理]) --> CheckFiles[检查EMF/WMF文件]
CheckFiles --> HasFiles{"找到文件?"}
HasFiles --> |否| SkipTask[跳过任务]
HasFiles --> |是| ConvertLoop[遍历文件列表]
ConvertLoop --> ConvertFile[转换单个文件]
ConvertFile --> UpdateProgress[更新进度]
UpdateProgress --> MoreFiles{"还有文件?"}
MoreFiles --> |是| ConvertLoop
MoreFiles --> |否| PatchMarkdown[更新Markdown引用]
PatchMarkdown --> UpdateContext[更新上下文]
UpdateContext --> End([处理完成])
SkipTask --> End
```

**图表来源**
- [mediaConvert.ts:42-111](file://src/tasks/mediaConvert.ts#L42-L111)

**章节来源**
- [mediaConvert.ts:42-111](file://src/tasks/mediaConvert.ts#L42-L111)

### 内容清理任务 (mdCleanupTask)

内容清理任务使用状态机算法清理 pandoc 生成的 Markdown 中的 HTML 标记。

```mermaid
stateDiagram-v2
[*] --> NORMAL : 初始状态
NORMAL --> IN_ZHENGWEN : 遇到正文段落
NORMAL --> IN_HEADING : 遇到标题
NORMAL --> IN_FIGURE : 遇到图片框
NORMAL --> IN_TABLE : 遇到表格
NORMAL --> IN_IMG : 遇到多行图片
IN_ZHENGWEN --> NORMAL : 遇到结束标签
IN_HEADING --> NORMAL : 遇到结束标签
IN_FIGURE --> NORMAL : 遇到结束标签
IN_TABLE --> NORMAL : 遇到结束标签
IN_IMG --> NORMAL : 图片标签闭合
note right of IN_HEADING
积累标题文本
转换为ATX格式
end note
note right of IN_FIGURE
提取图片源和标题
生成Markdown图片语法
end note
note right of IN_IMG
处理跨行图片标签
支持复杂HTML结构
end note
```

**图表来源**
- [mdCleanup.ts:6-14](file://src/tasks/mdCleanup.ts#L6-L14)
- [mdCleanup.ts:101-302](file://src/tasks/mdCleanup.ts#L101-L302)

**章节来源**
- [mdCleanup.ts:6-14](file://src/tasks/mdCleanup.ts#L6-L14)
- [mdCleanup.ts:101-302](file://src/tasks/mdCleanup.ts#L101-L302)

## 依赖分析

系统的关键依赖关系如下：

```mermaid
graph LR
subgraph "外部依赖"
LISTR[listr2<br/>任务编排]
INQUIRER[@inquirer/prompts<br/>交互式提示]
ADAPTER[@listr2/prompt-adapter-inquirer<br/>适配器]
end
subgraph "内部模块"
CONTEXT[context.ts<br/>接口定义]
RUNNER[runner.ts<br/>运行器]
TASKS[tasks/*<br/>业务逻辑]
UTILS[utils.ts<br/>工具函数]
end
LISTR --> RUNNER
INQUIRER --> TASKS
ADAPTER --> TASKS
RUNNER --> TASKS
TASKS --> CONTEXT
TASKS --> UTILS
CONTEXT -.-> TASKS
```

**图表来源**
- [package.json:21-25](file://package.json#L21-L25)
- [runner.ts:1-2](file://src/runner.ts#L1-L2)
- [docxInput.ts:1-3](file://src/tasks/docxInput.ts#L1-L3)

**章节来源**
- [package.json:21-25](file://package.json#L21-L25)

## 性能考虑

### 类型安全最佳实践

1. **可选属性处理**: 使用 `!` 操作符时要确保在访问前进行检查
2. **类型守卫**: 在访问 `lastContext` 之前进行类型检查
3. **接口约束**: 通过严格的接口定义防止意外的数据结构变化

### 常见错误避免

1. **空值访问**: 始终检查 `ctx.lastContext` 是否存在
2. **路径处理**: 区分绝对路径和相对路径的处理逻辑
3. **异步操作**: 正确处理 Promise 的错误情况

## 故障排除指南

### 常见问题及解决方案

| 问题类型 | 症状 | 解决方案 |
|----------|------|----------|
| Pandoc未安装 | 环境检查失败 | 安装pandoc并确保在PATH中 |
| 文件路径错误 | 输入验证失败 | 检查文件路径是否正确 |
| 权限不足 | 文件操作失败 | 检查目录权限设置 |
| 缓存损坏 | 输入缓存读取失败 | 删除缓存文件重新开始 |

**章节来源**
- [pandocCheck.ts:14-23](file://src/tasks/pandocCheck.ts#L14-L23)
- [docxInput.ts:13-25](file://src/tasks/docxInput.ts#L13-L25)

## 结论

AppContext 和 OutputContext 接口为整个 doc2xml-cli 工具提供了清晰的数据流架构。通过严格的类型定义和明确的职责分离，系统实现了高度的可维护性和扩展性。这些接口不仅定义了数据结构，更重要的是建立了任务间的协作机制，确保了整个转换流程的可靠性和一致性。